#![allow(dead_code)]
#![allow(unused_variables)]

extern crate tokio_proto;
extern crate bytes;
extern crate byteorder;

mod frames;

#[cfg(test)]
mod tests {
    #[test]
    fn lib_good() {
    	assert!(true);
    }
}
